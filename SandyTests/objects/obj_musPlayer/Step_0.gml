/// @description Insert description here
// You can write your code in this editor

/*
if(audio != -1 && !audio_is_playing(audio))
{
	//var trackPos = audio_sound_get_track_position(audio);
	var newPos = global.musInfo[global.currentMusic, 2];
	if(newPos != -1)
	{
		audio = audio_play_sound(global.musInfo[global.currentMusic, ablaze], 0, 0);
		audio_sound_set_track_position(audio, newPos);
	}
	else
	{
		audio = audio_play_sound(global.musInfo[global.currentMusic, ablaze], 0, 0);
		audio_sound_set_track_position(audio, 0);
	}
}
*/